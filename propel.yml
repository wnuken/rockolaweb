propel:
    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=localhost;dbname=rockolaweb
                user: developer
                password: developer
                settings:
                    charset: utf8
    runtime:
      defaultConnection: default
      connections:
          - default
    generator:
      defaultConnection: default
      connections:
          - default
